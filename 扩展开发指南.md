# CraftLink 扩展开发指南

本指南介绍如何为 CraftLink 编写扩展。扩展存放于 `mods/` 目录,每个扩展一个独立文件夹,入口文件必须命名为 `main.py`:

```
mods/
└── my_mod/
    └── main.py
```

服务启动时会自动扫描 `mods` 下所有目录的 `main.py`,完成校验后注册。

## 最小示例

```python
from core import data


class CraftLinkModsMain:
    class modsInfo:
        GDNDZZK = "CraftLink"
        version = "api_v1"
        name = "my_mod"

    def craftLinkEvent(self, evt: data):
        self.logger.info(f"t={evt.t} d={evt.d}")
```

注册成功后输出:

```
[INFO] [sys] [my_mod]:mods/my_mod/main.py 注册成功
```

## 结构要求

`main.py` 中必须定义 `CraftLinkModsMain` 类,该类需包含:

| 成员 | 说明 |
| --- | --- |
| `craftLinkEvent(self, evt)` | 事件回调,每个事件触发一次 |
| `modsInfo` | 嵌套类,声明扩展元信息 |

### modsInfo 字段

| 字段 | 要求 |
| --- | --- |
| `GDNDZZK` | 必须为 `"CraftLink"` |
| `version` | 必须为 `"api_v1"` |
| `name` | 任意字符串,作为日志来源显示 |

任一条件不满足时扩展不会注册,并输出对应 warn:

| 日志 | 原因 |
| --- | --- |
| `注册失败,协议不匹配` | 缺少 CraftLinkModsMain / craftLinkEvent / modsInfo,或 GDNDZZK 不正确 |
| `注册失败,错误的info` | modsInfo 缺少字段 |
| `注册失败,版本不匹配` | version 不是 api_v1 |

## 依赖声明与运行环境

每个插件在独立子进程中运行,与宿主及其他插件完全隔离。插件可声明自己的第三方依赖:

```
mods/
└── my_mod/
    ├── main.py
    └── requirements.txt   # 可选,标准 pip 格式
```

### 依赖自动安装

- 提供 `requirements.txt` 的插件,启动时框架会在 `mods` 同级的 `.venvs/<插件目录名>/` 下为其创建独立 venv 并安装依赖;requirements 内容变化时自动重建,无需手动干预
- `mods` 目录全程只读,所有 venv 与缓存都写入 `.venvs/`(插件目录被移除后对应的孤儿 venv 会在下次启动时自动清理)
- 未提供 `requirements.txt` 的插件直接使用宿主解释器,行为与之前一致
- 依赖安装失败(如包名错误、断网)时仅跳过该插件,不影响服务器启动
- 可用 `--no-auto-install` 禁用自动安装,此时带依赖声明的插件会被跳过并提示

### pip 镜像源选择

安装依赖时框架会从项目根目录 `mirrors.json` 中读取镜像候选列表(默认含官方源、阿里/腾讯/华为云及清华、中科大、北大、南大、浙大、上交、北外、华科、吉大、南科大、南工大等高校源),并发竞速探测,自动选用最快的镜像。探测结果缓存 24 小时(`.venvs/.mirror.json`),修改 `mirrors.json` 会立即失效重测。

优先级:`--index-url` 参数 > `PIP_INDEX_URL` 环境变量 > 自动探测 > pip 默认源。将 `mirrors.json` 改为空数组 `[]` 可禁用自动探测。

> 注意:自动镜像意味着信任第三方镜像站的分发完整性,对安全敏感的部署请用 `--index-url` 固定官方源。

### 运行模型与限制

- 插件崩溃或被杀死仅影响该插件本身,框架记录日志后继续运行,宿主与其他插件不受影响
- 事件数据经 JSON 序列化传给插件,因此 `evt.d` 必须是 JSON 可序列化内容
- 插件内的 `print` 输出会被重定向到框架日志,不会干扰进程间通信
- 异步回调在各自插件进程内按顺序执行

## 接收事件

事件以 `data` 实例传入 `craftLinkEvent`:

- `evt.t`:事件类型(str)
- `evt.d`:事件数据,无数据时为 `None`

服务启动后会向每个扩展发送初始化事件:

```python
def craftLinkEvent(self, evt: data):
    if evt.t == "craftLinkInit":
        ...
        return
```

`craftLinkEvent` 支持同步与异步两种写法:

```python
async def craftLinkEvent(self, evt: data):
    await do_something()
```

回调内抛出的任何异常都会被框架捕获,以 err 级别输出扩展名、错误信息与堆栈,不影响其它扩展。

## 日志

框架在注册校验通过后向实例注入 `self.logger`,直接使用即可:

```python
self.logger.info("已就绪")
self.logger.warn("配置缺失")
self.logger.err("发生错误")
```

输出格式:

```
[2026-08-23 19:38:18] [INFO] [my_mod] 已就绪
```

日志来源为注册时由框架记录的 `modsInfo.name`,不受扩展运行期代码影响;请勿自行构造 `Logger` 替代。框架自身日志的来源显示为 `sys`。
